NGFOR

1. Vamos a la carpeta app 

2. En el archivo app.component.html se crea una tabla dentro de un div
dentro de la etiqueta de las filas <tr> se crea el ngFor asi:  

<tr *ngFor= "let estudiante of listEstudiantes; let i = index ">

ejemplo de tabla de estudiantes:

<div class="container">
  <div class="row">
    <h3>LISTADO DE ESTUDIANTES</h3>
  </div>
  <br>
  <table class="table" *ngIf="listEstudiantes.length > 0">
    <thead>
      <th>Nro</th>
      <th>Nombre</th>
      <th>Estados</th>
    </thead>
    <tbody>
      <tr *ngFor= "let estudiante of listEstudiantes; let i = index ">
        <td>{{ i + 1}}</td>
        <td>{{ estudiante.nombre }}</td>
        <td>{{ estudiante.estado }}</td>
      </tr>
    </tbody>
  </table>
  <h3 *ngIf="listEstudiantes.length === 0">No hay registros para mostrar</h3>

</div>

3. vamos al archivo app.component.ts

dentro de export class AppComponent línea 12 se crea la lista de estudiantes, asi:

export class AppComponent {
  listEstudiantes: any[] = [
    { nombre: "Tomas Gonzalez", estado: "Promocionado" },
    { nombre: "Lucas Perez", estado: "Regular" },
    { nombre: "Juan Garcia", estado: "Regular" },
    { nombre: "Marta Perez", estado: "Promocionado" },
  ]
  mostrar = true;

  toogle(): void {
    this.mostrar = !this.mostrar;
  }
}


tabla

archivo html

<div class="container">
  <div class="row">
    <h3>LISTADO DE ESTUDIANTES</h3>
  </div>
  <br>
  <table class="table" *ngIf="listEstudiantes.length > 0">
    <thead>
      <th>Nro</th>
      <th>Nombre</th>
      <th>Estados</th>
    </thead>
    <tbody>
      <tr *ngFor= "let estudiante of listEstudiantes; let i = index " [ngSwitch]="estudiante.estado">
        <td>{{ i + 1}}</td>
        <td>{{ estudiante.nombre }}</td>
        <td>
          <!-- <span [ngClass]="estudiante.estado === 'Promocionado' ? 'badge badge-success' : 'badge badge-secondary'">
            {{ estudiante.estado }}
          </span>  para resaltar dos colores -->
          <span *ngSwitchCase="'Promocionado'" class="badge badge-success">
            {{ estudiante.estado }}
          </span>
          <span *ngSwitchCase="'Regular'" class="badge badge-secondary">
            {{ estudiante.estado }}
          </span>
          <span *ngSwitchCase="'Libre'" class="badge badge-danger">
            {{ estudiante.estado }}
          </span>
        </td>
      </tr>
    </tbody>
  </table>
  <h3 *ngIf="listEstudiantes.length === 0">No hay registros para mostrar</h3>

</div>


EJERCICIO TABLA NG FOR NG SWITCH ...
app.component.html

<div class="container">
  <div class="row">
    <h3>LISTADO DE ESTUDIANTES</h3>
  </div>
  <br>
  <table class="table" *ngIf="listEstudiantes.length > 0">
    <thead>
      <th>Nro</th>
      <th>Nombre</th>
      <th>Estados</th>
    </thead>
    <tbody>
      <tr *ngFor= "let estudiante of listEstudiantes; let i = index " [ngSwitch]="estudiante.estado">
        <td>{{ i + 1}}</td>
        <td>{{ estudiante.nombre }}</td>
        <td>
          <!-- <span [ngClass]="estudiante.estado === 'Promocionado' ? 'badge badge-success' : 'badge badge-secondary'">
            {{ estudiante.estado }}
          </span>  para resaltar dos colores -->
          <span *ngSwitchCase="'Promocionado'" class="badge badge-success">
            {{ estudiante.estado }}
          </span>
          <span *ngSwitchCase="'Regular'" class="badge badge-secondary">
            {{ estudiante.estado }}
          </span>
          <span *ngSwitchCase="'Libre'" class="badge badge-danger">
            {{ estudiante.estado }}
          </span>
        </td>
      </tr>
    </tbody>
  </table>
  <h3 *ngIf="listEstudiantes.length === 0">No hay registros para mostrar</h3>

</div>


app.component.ts


import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-root',
  standalone: true,
  imports: [CommonModule, RouterOutlet],
  templateUrl: './app.component.html',
  styleUrl: './app.component.css'
})
export class AppComponent {
  listEstudiantes: any[] = [
    { nombre: "Tomas Gonzalez", estado: "Promocionado" },
    { nombre: "Lucas Perez", estado: "Regular" },
    { nombre: "Patricia Funez", estado: "Libre" },
    { nombre: "Juan Garcia", estado: "Regular" },
    { nombre: "Marta Perez", estado: "Promocionado" },
    { nombre: "Nicolas Gomez", estado: "Libre" },

  ]
  mostrar = true;

  toggle(): void {
    this.mostrar = !this.mostrar;
  }
}



